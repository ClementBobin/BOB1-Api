// JwtOptions lives in Infrastructure.Configuration — alias it here so
// Api code can still use the short name without an extra using.
global using JwtOptions = Infrastructure.Configuration.JwtOptions;
